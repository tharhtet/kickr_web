# Base URL of the kickr NestJS backend. Only read when VITE_USE_MOCKS is not "true".
VITE_API_BASE_URL=http://localhost:3000

# When "true", the app serves the in-repo mock fixtures instead of calling the API.
# The scaffold ships with this on so the UI runs with no backend.
VITE_USE_MOCKS=true
